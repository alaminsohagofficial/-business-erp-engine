version: "1"
services:
  - type: web
    name: business-erp-engine
    runtime: node
    repo: https://github.com/alaminsohagofficial/-business-erp-engine
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: node server.js
    autoDeployTrigger: commit
